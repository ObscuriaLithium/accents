{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "homepage": "${curseforge}",
    "sources": "${sources}",
    "issues": "${issues}"
  },
  "license": "${license}",
  "icon": "${mod_id}.png",
  "accessWidener": "${mod_id}.accesswidener",
  "environment": "*",
  "entrypoints": {
    "main": [
      "dev.obscuria.accents.fabric.FabricAccents"
    ],
    "client": [
      "dev.obscuria.accents.fabric.client.FabricAccentsClient"
    ],
    "jei_mod_plugin": [
      "dev.obscuria.accents.compat.JEIAccentsPlugin"
    ]
  },
  "mixins": [
    "${mod_id}.mixins.json",
    "${mod_id}.fabric.mixins.json"
  ],
  "depends": {
    "java": ">=${java_version}",
    "minecraft": "~${minecraft_version}",
    "fragmentum": ">=${fragmentum_version}",
    "fabricloader": ">=${fabric_loader_version}",
    "geckolib": ">=5.4.0",
    "fabric-api": "*"
  },
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord}"
      }
    }
  }
}
  